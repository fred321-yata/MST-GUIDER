import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.schoolguider.seait',
  appName: 'MST Guider (SEAIT)',
  webDir: 'dist',
}

export default config
